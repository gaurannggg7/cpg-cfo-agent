fastapi==0.109.0
uvicorn==0.27.0
groq==1.4.0
pandas==2.2.0
python-dotenv==1.2.2
python-multipart==0.0.6
langgraph==0.2.0

# Kafka consumer (kafka_consumer.py)
aiokafka==0.10.0

# Prometheus metrics (monitoring.py)
prometheus-client==0.8.0

# Prometheus FastAPI middleware (main.py) — auto-instruments request
# count/latency and exposes /metrics on the app's own port.
prometheus-fastapi-instrumentator==5.1.0

# Firestore evaluation logging (evaluation.py)
firebase-admin==6.5.0
